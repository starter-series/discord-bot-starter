# Copy this file to fly.toml and update the app name.
# Discord bots are worker processes — no HTTP services needed.

app = "your-bot-name"
primary_region = "nrt"

[build]

[env]
  NODE_ENV = "production"
